# Cloudflare Pages redirects - this deployment publishes the repo ROOT
# (assets at /index.html, /icons/, ...).

# SPA fallback: any unknown path serves the shell (index.html).
# Note: the /api/* calls go DIRECTLY to https://morouj-plant-backend.onrender.com
# via config.js (MOROUJ_API_BASE + CORS); Cloudflare's _redirects proxy does
# not support external domains, so no proxy rule is used here.
/* /index.html 200
// Morouj Plant App - Frontend Configuration
// Base URL for the FastAPI backend (Render). The browser calls the API
// cross-origin; the backend must allow this origin via CORS_ORIGINS.
var MOROUJ_API_BASE = "https://morouj-plant-backend.onrender.com";
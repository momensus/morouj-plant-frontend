// Morouj Plant App - Frontend Configuration
// Override this before deploying the frontend (Cloudflare Pages).
// Empty ("" => same origin) when the Cloudflare _redirects proxy forwards /api/*
// to the Render backend. Set it to the backend URL only if you use direct
// cross-origin API calls (and then set CORS_ORIGINS on the backend accordingly).
var MOROUJ_API_BASE = "";